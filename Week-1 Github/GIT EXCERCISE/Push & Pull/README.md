# Push & Pull

## Objective

To pull the latest changes from the remote repository and push local commits to GitHub.

## Commands Used

```bash
git status
git branch -a
git pull origin main
git push origin main
```

## Result

The local repository was synchronized with the remote repository, and all local commits were pushed successfully.
