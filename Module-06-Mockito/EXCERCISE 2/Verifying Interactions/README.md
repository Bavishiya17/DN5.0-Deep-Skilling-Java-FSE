# Exercise 2: Verifying Interactions

## Scenario
Ensure that a method is called with specific arguments by verifying interactions on a mock.

## Steps
1. Create a mock object.
2. Call the method with specific arguments.
3. Verify the interaction using erify().

## Concepts
- erify(mock).method() - asserts a method was called
