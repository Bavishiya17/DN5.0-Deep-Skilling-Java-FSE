# Exercise 3: Argument Matching

## Scenario
Verify that a method is called with specific arguments using Mockito argument matchers.

## Steps
1. Create a mock object.
2. Call the method with specific arguments.
3. Use argument matchers to verify the interaction.

## Concepts
- nyString(), eq(), ny() - argument matchers
- erify(mock).method(anyString()) - flexible verification
